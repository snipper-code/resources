name: CI on any branch master created

on:
  release:
    types: [published]
    branches:
      - release  

jobs:
  build:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Build
        run: |
          echo "Tag is a match for release"
