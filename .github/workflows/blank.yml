name: CI on any branch master created

on:
  release:
    types: [published]
    branches:
      - master  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          echo "Tag is a match"
